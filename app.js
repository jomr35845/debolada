// Seleccionamos el botón
    const boton = document.getElementById("irPagina");

    // Agregamos el event listener
    boton.addEventListener("click", () => {
      // Redirigir a otra página
      window.location.href = "https://www.google.com";
    });